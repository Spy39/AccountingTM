@* @model ErrorViewModel *@
@{
    ViewData["Title"] = "Ошибка";
}

<div class="container mt-5">
    <div class="row justify-content-center">
        <div class="col-md-8">
            <div class="card shadow-sm border-danger">
                <div class="card-header bg-danger text-white text-center">
                    <h4><i class="fas fa-exclamation-triangle"></i> Ошибка</h4>
                </div>
                <div class="card-body text-center">
                    <h5 class="text-danger">Произошла ошибка при обработке запроса.</h5>

                    @if (Model.ShowRequestId)
                    {
                        <p class="text-muted mt-2">
                            <strong>Идентификатор запроса:</strong> <code>@Model.RequestId</code>
                        </p>
                    }

                    <hr>

                    <h6 class="text-muted">Режим разработки</h6>
                    <p class="small text-muted">
                        Переключение в режим <strong>Development</strong> позволит отобразить больше деталей об ошибке.
                        <br>
                        <strong>Не включайте этот режим в рабочем окружении!</strong>
                        Это может привести к утечке чувствительных данных.
                    </p>

                    <div class="d-grid gap-2 d-md-flex justify-content-md-center">
                        <a href="/" class="btn btn-outline-primary">
                            <i class="fas fa-home"></i> Вернуться на главную
                        </a>
                        <button class="btn btn-outline-secondary" onclick="location.reload();">
                            <i class="fas fa-redo"></i> Обновить страницу
                        </button>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>
